name: release-please
on:
  push:
    branches:
      - main
jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: GoogleCloudPlatform/release-please-action@v2.35.0
        with:
          release-type: node
          package-name: ctrl-q-cli
          default-branch: main
